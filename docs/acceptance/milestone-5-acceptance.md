# Milestone 5 Acceptance

## Purpose

This file defines acceptance examples for Milestone 5: Apple App Shell / Usable Vertical Slice.

## Acceptance Fixture Set

### A-M5-LAUNCH-001: macOS App Launch

Given the package is opened in Xcode, when the `DreamJotterMac` scheme runs, then a macOS window opens with the Project Library screen.

Traceability: M5-APPLE-APP-SHELL.

### A-M5-CREATE-001: Create Blank Project

Given the Project Library is visible, when the user enters a title and creates a project, then the app opens an editable project document backed by `DreamJotterCore`.

Traceability: M5-APPLE-APP-SHELL, M1-APPLE-PROTOTYPE-FOUNDATIONS.

### A-M5-EDITOR-001: Temporary TextEditor Parses Screenplay

Given an editable project is open, when the user types Fountain-like screenplay text, then `ScreenplayParser` reparses it and scene and character lists update from semantic core data.

Traceability: M5-APPLE-APP-SHELL, M1-APPLE-PROTOTYPE-FOUNDATIONS.

### A-M5-DASHBOARD-001: Dashboard Summary

Given a project is open, when screenplay text or project data changes, then the dashboard shows title, logline when present, scene count, character count, and note count.

Traceability: M5-APPLE-APP-SHELL, M2-REAL-MVP-WRITER-ORGANIZATION.

### A-M5-STORAGE-001: Save And Open Package

Given a project is open, when the user saves it as a `.dreamjotter` package, then `DreamJotterPackageStore` writes canonical package data. When the package is opened later, the app restores the project and editor text from package data.

Traceability: M5-APPLE-APP-SHELL, STORAGE-DREAMJOTTER-PACKAGE-FORMAT.

### A-M5-EXPORT-001: Export Fountain

Given a project is open, when the user exports Fountain, then the app writes Fountain text generated by `FountainIO`.

Traceability: M5-APPLE-APP-SHELL, EDITOR-FOUNTAIN-SUPPORT.

### A-M5-HEALTH-001: Health Report

Given a project is open, when the app renders the inspector, then script health findings are shown using existing health-report core behavior.

Traceability: M5-APPLE-APP-SHELL, SCRIPT-ANALYSIS-SPEC.

### A-M5-ERRORS-001: Simple Error Presentation

Given load, save, or export fails, when the error reaches the app shell, then the user sees a simple error alert and project data remains unchanged.

Traceability: M5-APPLE-APP-SHELL.

## Deferred Acceptance

- TextKit/AppKit screenplay editor adapter.
- iPadOS and iOS app targets.
- UI automation tests.
- SwiftData recents/cache.
- iCloud sync.
- Plugin runtime.
- Real AI providers.
